# Variables para Docker (compose en la raíz). En VPS: copiar a la raíz como .env y editar.
# No subir .env a Git.
#
DB_NAME=campos_libres_db
DB_USER=postgres
DB_PASSWORD=tu_contraseña_secreta
#
JWT_SECRET=este_es_un_secreto_muy_largo_y_seguro_para_jwt
#
# Producción con Nginx Proxy Manager
# FRONTEND_URL=https://camposlibres.ibstudiosoluciones.cloud
